const express = require('express');
const router = express.Router();

// Placeholder for Phase 4
router.get('/', (req, res) => {
  res.json({ message: 'Agent routes - Phase 4' });
});

module.exports = router;
